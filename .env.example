# NOVARIS — variáveis de ambiente (estrutura apenas; nenhum valor real)
# Copie para .env.local e preencha. Nunca commitar .env.local (ver PROJECT_RULES.md § Regras de Segurança).

# --- Database (packages/database — Prisma) ---
DATABASE_URL=

# --- Supabase (ver architecture/stack-tecnologica.md) ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# --- Auth (services/kernel/identity) ---
# TODO — mecanismo concreto ainda não definido (ver services/kernel/identity/CONTRACT.md)

# --- IA (services/kernel/ai-runtime) ---
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# --- Automação (services/kernel/automation-runtime) ---
# TODO — integrações concretas ainda não definidas

# --- Integrações Externas (services/kernel/integration-hub, ADR-0040) ---
# Estrutura pronta (Ports + Console adapters), nenhuma credencial existe ainda.
# Preencher aqui é o único passo que falta para trocar cada Console adapter
# por um adapter HTTP real, sem mudar nenhum Port/Controller.
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
META_ACCESS_TOKEN=
META_PAGE_ID=
BLING_API_KEY=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=
