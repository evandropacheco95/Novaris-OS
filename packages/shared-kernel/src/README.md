# src

Código-fonte do `shared-kernel`. Ver [../README.md](../README.md) para a estrutura completa (`core/` — Domain Layer — e as 9 pastas cross-cutting).

`index.ts` é o barrel de exportação pública, vazio até o primeiro módulo real.
