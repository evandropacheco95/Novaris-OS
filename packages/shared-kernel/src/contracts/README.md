# contracts

## Objetivo

Contratos de interface internos ao Shared Kernel (ex.: assinaturas de porta usadas por múltiplos blocos de `core/`) — distinto de [packages/contracts/](../../../contracts/README.md), que cobre eventos/API/schemas de comunicação entre `services/kernel/` e `services/domains/`.

## Status

🚧 Estrutura criada (Missão ENG-0001.1). Nenhum contrato implementado ainda.
