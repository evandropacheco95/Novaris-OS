{
  "name": "@novaris/organizations",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src tests --ext .ts",
    "test": "tsc -p tsconfig.json && cd dist && node --test"
  },
  "dependencies": {
    "@novaris/audit": "workspace:^",
    "@novaris/database": "workspace:^",
    "@novaris/shared-kernel": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "eslint": "^8.57.0",
    "typescript": "^5.5.0"
  }
}
